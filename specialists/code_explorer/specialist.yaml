agentic: true
max_iterations: 15
description: "Codebase navigation and comprehension — answers 'how does X work?', traces dependencies, maps call graphs, explains architecture. READ-ONLY analysis, does not produce documentation → doc_generator."
preset: "code"
tools:
  - terminal_execute
  - read_file
  - search_files
mcp_servers: []
directive_types:
  - code
  - architecture
  - self_aware
