layers:
- pl_boto3
- pl_master_copy
memory: 256
timeout: 120
description: Seed per-user copies of master competitions and federations.
input_schema:
  type: object
  properties:
    pk:
      type: string
      description: User partition key (defaults to env HEALTH_PROGRAM_PK).
  required: []

reserved_concurrency: null
