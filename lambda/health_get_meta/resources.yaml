layers:
- pl_boto3
- pl_program
- pl_sessions
min_replicas: 0

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 300m
    memory: 256Mi
timeout: 900
description: Get program metadata (name, dates, weight class, etc.).
input_schema:
  type: object
  properties: {}
  required: []

reserved_concurrency: null
