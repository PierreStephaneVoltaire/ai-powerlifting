layers:
- pl_boto3
- pl_analysis_cache
resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 300m
    memory: 256Mi
timeout: 60
description: Mark a user's markdown export as dirty so the next read regenerates it.
input_schema:
  type: object
  properties:
    pk:
      type: string
      description: User partition key (defaults to env HEALTH_PROGRAM_PK).
    block_key:
      type: string
      description: Block key (defaults to "current").
      default: current
    reason:
      type: string
      description: Free-text reason for marking dirty.
  required: []

reserved_concurrency: null
