# pl-templates — DynamoDB domain Lambda layer for the "if-health-templates" table.
#
# Pure-python layer: ships only template_store.py (copied verbatim from
# tools/health/). There are no pip dependencies for this layer.
#
# boto3 / botocore / s3transfer are provided by the pl-boto3 layer, which
# lambdas attach alongside this one. Do not add those here.