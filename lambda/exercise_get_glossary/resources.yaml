layers:
- pl_boto3
- pl_glossary
memory: 256
timeout: 900
description: Get the full exercise glossary (exercises, updated_at).
input_schema:
  type: object
  properties:
    pk:
      type: string
      description: Partition key (defaults to env HEALTH_PROGRAM_PK).
  required: []

reserved_concurrency: null