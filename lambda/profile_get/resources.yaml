layers:
- pl_boto3
memory: 256
timeout: 900
description: Build a public profile by nickname with program + videos.
input_schema:
  type: object
  properties:
    nickname:
      type: string
      description: Profile nickname to look up.
    viewer_username:
      type: string
      description: Discord username of the requesting user.
  required:
  - nickname

reserved_concurrency: null