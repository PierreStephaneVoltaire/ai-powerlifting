layers:
- pl_boto3
- pl_glossary
resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 300m
    memory: 256Mi
timeout: 900
description: Insert or update an exercise in the glossary by id.
input_schema:
  type: object
  properties:
    pk:
      type: string
      description: Partition key (defaults to env HEALTH_PROGRAM_PK).
    exercise:
      type: object
      description: 'Exercise dict: {id?, name, primary_muscles, ...}'
  required:
  - exercise

reserved_concurrency: null