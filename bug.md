# bug.md

- frontend/src/components/budget/BudgetTimeline.tsx — references old pre-BUD-01 schema: `item.recurrence === 'one_time'` (should be `'ONE_TIME'`), `needed_for_comp_day` (removed; use `comp_linked`/`priority_tier`), `comp_master_ids` (removed; use `comp_linked`/`competition_id`), `transportation` category (renamed to `transport`), `config.monthly_budget` (renamed to `config.monthly_cap`). BUD-04 needs to migrate to the canonical `@powerlifting/types` BudgetItem/BudgetConfig. [RESOLVED by BUD-04: component now imports from canonical `@powerlifting/types` + `budgetShared`/`dateUtils` shared modules; uses `monthly_cap`, `'ONE_TIME'`, `priority_tier`, `comp_linked`; wired into BudgetPage timeline tab with `athleteName` resolved from `getSettings()` (resolved pk, defaults to `operator` when unauthenticated for demo).]
- frontend/src/api/settings.ts — `UserSettings` interface was missing `pk` and `mapped_pk` fields that the backend `UserSettings` returns. [FIXED by BUD-04: added `pk: string` and `mapped_pk?: string` to the frontend interface.]
- FIXED (parent stack terraform/k8s-ingress.tf, security-only SnippetsFilter): CSP `default-src 'self'` had no `img-src`, so avatar/media images from `cdn.discordapp.com` and `*.cloudfront.net` were blocked. Added `img-src 'self' data: blob: https://cdn.discordapp.com https://*.cloudfront.net`. Apply the parent stack to deploy.