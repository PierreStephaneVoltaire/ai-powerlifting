# bug.md

- frontend/src/components/budget/BudgetTimeline.tsx — references old pre-BUD-01 schema: `item.recurrence === 'one_time'` (should be `'ONE_TIME'`), `needed_for_comp_day` (removed; use `comp_linked`/`priority_tier`), `comp_master_ids` (removed; use `comp_linked`/`competition_id`), `transportation` category (renamed to `transport`), `config.monthly_budget` (renamed to `config.monthly_cap`). BUD-04 needs to migrate to the canonical `@powerlifting/types` BudgetItem/BudgetConfig.