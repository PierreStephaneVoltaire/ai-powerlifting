# bug.md

- frontend/src/pages/BudgetPage.tsx — old Items tab helpers (defaultRecurrence, makeBlankItem, itemActiveInMonth, monthCostForItem, buildMonths, spentByMonth) and ItemCard/CategoryFields still reference the pre-BUD-01 schema (recurring/one_time, monthly_budget, comp_master_ids, needed_for_comp_day, equipment_condition, equipment_comp_legal, transportation). [RESOLVED by BUD-03: ItemsTab/ItemCard/CategoryFields/PhotoSlot dead code removed; Items tab now uses BudgetTable with a local V2 contract + legacyToV2/v2ToLegacy boundary so BUD-03 is independent of BUD-01's unstable shared types]
- frontend/src/components/budget/BudgetTimeline.tsx — imports `recurrenceOf` from budgetShared which was removed (BudgetItem.recurrence is now the canonical enum). BUD-04 needs to use item.recurrence directly. [RESOLVED by BUD-04: component is now self-contained — no shared-module deps; works against the current canonical (legacy) `@powerlifting/types` schema]
- frontend/src/store/budgetStore.ts — DEFAULT_CONFIG uses `{ monthly_budget: 0, currency: 'CAD' }` which matches the current (reverted) `@powerlifting/types` BudgetConfig. [No longer a bug — BUD-01 reverted the types back to the legacy schema]
- frontend/src/components/budget/budgetShared.ts — references `BudgetPriorityTier` and `BudgetRecurrence` (new-schema enums: MANDATORY/IMPORTANT/OPTIONAL, ONE_TIME/MONTHLY/QUARTERLY/ANNUAL) and `item.priority_tier`/`item.comp_linked`/`config.monthly_cap` which no longer exist on the reverted `@powerlifting/types` BudgetItem/BudgetConfig. Broken until BUD-01/BUD-02/BUD-03 reconcile with the reverted schema. BUD-04 does NOT depend on this module.
- frontend/src/components/budget/dateUtils.ts — references `item.recurrence === 'ONE_TIME'` and `item.date_precision` which don't exist on the reverted BudgetItem (uses lowercase `one_time`/`recurring`, no date_precision). Broken until reconciled. BUD-04 does NOT depend on this module.
- frontend/src/components/budget/types.ts — references `BudgetPriorityTier`, `BudgetDatePrecision`, `comp_linked`, `priority_tier`, `date_precision` from the new schema that was reverted from `@powerlifting/types`. Broken until reconciled. BUD-04 does NOT depend on this module.
- frontend/src/components/budget/ExpenseRow.tsx — uses Mantine `TableCell`/`TableRow` (renamed to `TableTd`/`TableTr` in Mantine 9) and `Collapse` `in` prop. [RESOLVED by BUD-03: uses Table.Tr/Table.Td via Table namespace import; Collapse uses `expanded` prop]
- frontend/src/components/budget/budgetConstants.tsx — imports non-existent `BedSandwich` from lucide-react. [RESOLVED by BUD-03: uses `Sandwich`]
- frontend/src/components/budget/types.ts — references old BudgetItem fields (comp_master_ids, photo_url, equipment_condition, needed_for_comp_day, transportation). [RESOLVED by BUD-03: local V2 contract with legacyToV2/v2ToLegacy boundary; self-contained, does not depend on @powerlifting/types budget schema]